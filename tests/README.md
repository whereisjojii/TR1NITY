# `tests/` — Cross-service integration tests

Per-service unit tests live alongside their service code (`services/<name>/tests/`). This folder holds end-to-end tests that exercise the full stack via `make demo`.
