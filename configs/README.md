# `configs/` — Runtime configuration

| File | Purpose |
|------|---------|
| `wazuh/` | Wazuh manager + indexer config overrides |
| `opensearch/` | Index State Management (ISM) policies |
| `modsecurity/` | OWASP CRS overrides |
| `whitelists/` | Layer-1 deterministic FP whitelists (YAML) |
| `runbooks/` | Markdown runbooks (Phase 4) |
