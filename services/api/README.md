# `api` service

Phase 0 placeholder — full implementation arrives in the phase listed in [`../../ROADMAP.md`](../../ROADMAP.md).

```bash
# from the repo root
docker compose -f deploy/docker-compose.yml up api
```
